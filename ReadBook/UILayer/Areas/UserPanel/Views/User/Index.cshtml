@using ApplicationLayer.Models.ViewModels
@model IEnumerable<CornerVM>
<h1>Okuma Köşem</h1>

<table class="table table-striped table-hover">
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.BookName</td>
            <td>@item.BookSummary</td>
            
        </tr>
        
    }



</table>